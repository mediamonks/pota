import{_ as e,c as t,o as n,a}from"./app.0975428b.js";const w='{"title":"Twig Support","description":"","frontmatter":{},"headers":[{"level":2,"title":"Remove Twig","slug":"remove-twig"},{"level":2,"title":"Enable Twig","slug":"enable-twig"},{"level":2,"title":"Twing","slug":"twing"},{"level":2,"title":"Twing-loader","slug":"twing-loader"},{"level":2,"title":"Pota config","slug":"pota-config"},{"level":2,"title":"Storybook middleware","slug":"storybook-middleware"},{"level":2,"title":"Extending twig","slug":"extending-twig"},{"level":2,"title":"Pages bundle","slug":"pages-bundle"},{"level":2,"title":"Twig rendering on a preview server for Storybook","slug":"twig-rendering-on-a-preview-server-for-storybook"}],"relativePath":"combinations/muban/twig.md","lastUpdated":1681473882000}',o={},s=a(`<h1 id="twig-support" tabindex="-1">Twig Support <a class="header-anchor" href="#twig-support" aria-hidden="true">#</a></h1><p>Besides the default TypeScript templates that is the preferred way to start a project, the skeleton also has support for <a href="https://twig.symfony.com/" target="_blank" rel="noopener noreferrer">Twig</a> files. By default this is disabled, but the files do exist in the skeleton.</p><p>When starting a project, you have the choice:</p><ol><li>Remove Twig, because you&#39;re not going to use it.</li><li>Enable Twig, because you want to make use of it.</li></ol><h2 id="remove-twig" tabindex="-1">Remove Twig <a class="header-anchor" href="#remove-twig" aria-hidden="true">#</a></h2><p>The project contains a script that will remove all the twig related files. You can run the script by executing:</p><div class="language-sh"><pre><code>node scripts/clean-twig.js
</code></pre></div><p>And then commit your changes.</p><blockquote><p>If you don&#39;t remove the twig files, it won&#39;t do any harm, except for cluttering your filesystem. As long as the <code>config.twig-support</code> in the <code>package.json</code> is set to <code>false</code>, it doesn&#39;t use any of the twig related files.</p></blockquote><h2 id="enable-twig" tabindex="-1">Enable Twig <a class="header-anchor" href="#enable-twig" aria-hidden="true">#</a></h2><p>Edit your <code>package.json</code> file to set <code>config.twig-support</code> to <code>true</code>.</p><div class="language-json"><pre><code><span class="token comment">// package.json</span>
<span class="token punctuation">{</span>
  ...
  <span class="token property">&quot;config&quot;</span><span class="token operator">:</span> <span class="token punctuation">{</span>  
    <span class="token property">&quot;twig-support&quot;</span><span class="token operator">:</span> <span class="token boolean">true</span>
  <span class="token punctuation">}</span><span class="token punctuation">,</span>
  ...
<span class="token punctuation">}</span>
</code></pre></div><p>With this enabled, it starts executing the twig code paths, and your typescript templates are not used anymore (unless you don&#39;t switch to server-side rendering in storybook).</p><h1 id="how-does-it-all-work" tabindex="-1">How does it all work? <a class="header-anchor" href="#how-does-it-all-work" aria-hidden="true">#</a></h1><p>The <code>muban-webpack-scripts</code> package has built-in support for twig templates, by enabling a few small additions.</p><h2 id="twing" tabindex="-1">Twing <a class="header-anchor" href="#twing" aria-hidden="true">#</a></h2><p>It uses <a href="https://www.npmjs.com/package/twing" target="_blank" rel="noopener noreferrer">Twing</a>, a node.js package that aligns with the official Twig specification, allowing us to write templates in twig, but still make use of javascript-based build tooling.</p><p>Twing works by setting up a <em>Twig Environment</em>, that defines how files are loaded and cached, and allows you to add your custom Twig filters and functions.</p><p>The <code>muban-webpack-scripts</code> packages has its own <code>twigEnvironment.js</code> file, but most likely you want to have your own in your project to add custom filters and functions.</p><h2 id="twing-loader" tabindex="-1">Twing-loader <a class="header-anchor" href="#twing-loader" aria-hidden="true">#</a></h2><p>It enables the <a href="https://www.npmjs.com/package/twing-loader" target="_blank" rel="noopener noreferrer">twing-laoder</a> when rendering pages, so your <code>app.twig</code> and all other templates are correctly processed.</p><h2 id="pota-config" tabindex="-1">Pota config <a class="header-anchor" href="#pota-config" aria-hidden="true">#</a></h2><p>Our project contains a default <code>pota.config.js</code> to configure our Twig Environment. It does so by overriding the <code>twigEnvironmentPath</code> getter:</p><div class="language-ts"><pre><code><span class="token comment">// pota.config.js</span>
<span class="token keyword">class</span> <span class="token class-name">ProjectWebpackConfig</span> <span class="token keyword">extends</span> <span class="token class-name">MubanWebpackConfig</span> <span class="token punctuation">{</span>
  <span class="token keyword">get</span> <span class="token function">twigEnvironmentPath</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>  
    <span class="token keyword">return</span> <span class="token keyword">require</span><span class="token punctuation">.</span><span class="token function">resolve</span><span class="token punctuation">(</span><span class="token string">&#39;./config/twig/twigEnvironment.cjs&#39;</span><span class="token punctuation">)</span>  
  <span class="token punctuation">}</span>
<span class="token punctuation">}</span>
</code></pre></div><blockquote><p>The pota configuration is used in the <code>dev</code> and <code>build</code> commands.</p></blockquote><h2 id="storybook-middleware" tabindex="-1">Storybook middleware <a class="header-anchor" href="#storybook-middleware" aria-hidden="true">#</a></h2><p>In <code>.storybook/middleware.js</code> \u2013 that allows you to enhance the express node.js server that servces storybook itself \u2013 there is a route that can render templates &quot;on the server&quot;.</p><p>In our storybook and stories we can configure it to render story templates on the server by executing a <code>fetch</code> request with the component ID and parameters, that is then handled by the storybook middleware.</p><p>There it renders the component twig file, and returns the HTML to display in storybook.</p><p>The storybook middleware also creates a Twig Environment, and enhances it with the same extensions as is done in the <code>twigEnvironment.cjs</code> that is used in the pota config.</p><h2 id="extending-twig" tabindex="-1">Extending twig <a class="header-anchor" href="#extending-twig" aria-hidden="true">#</a></h2><p>The <code>config/twig/twig-extensions.cjs</code> contains a <code>addExtensions</code> function to allow extending the passed environment. You can use the <code>env.addFilter</code> and <code>env.addFunction</code>, and pass the configuration and logic.</p><p>Some things to note:</p><ul><li>The code is <code>commonjs</code> to work in all environments.</li><li>Added filters/functions should return a promise.</li><li>Objects defined in the twig templates are converted to <code>Map</code>s, so you might want to convert those parameters back to an <code>object</code>.</li><li>If you&#39;re outputting &quot;raw html&quot;, you need to mark it as safe so it doesn&#39;t get escaped.</li><li>Any custom filter or function you&#39;re adding must also be created in PHP, so make sure to add proper documentation and/or tests.</li></ul><h2 id="pages-bundle" tabindex="-1">Pages bundle <a class="header-anchor" href="#pages-bundle" aria-hidden="true">#</a></h2><p>When using typescript, the <code>src/pages/_main.ts</code> is used to export the <code>App.template.ts</code> (the application starting point) and all the page data files.</p><p>A similar file exists for twig, called <code>src/pages/_main.twig.ts</code> that exports the <code>app.twig</code> and all the same page data files, but additionally requires all twig templates in the projects to make them available for inclusion in other templates.</p><h2 id="twig-rendering-on-a-preview-server-for-storybook" tabindex="-1">Twig rendering on a preview server for Storybook <a class="header-anchor" href="#twig-rendering-on-a-preview-server-for-storybook" aria-hidden="true">#</a></h2><p>When deploying a storybook build to a preview server, it still needs a way to render the twig templates. If you choose to connect to an existing server that can render twig templates, you don&#39;t have to do anything. But otherwise you would need to run a node.js server that can render the templates.</p><p>As part of the <code>storybook:build</code> npm script, the file to run this node server is created in the <code>dist/node</code> folder, along with all the twig templates and custom helpers you added. Then you can run the <code>rsync:node</code> to rsync it to your preview server.</p><p>On the server you have to start the node server by starting <code>node twig.mjs</code>. It will automatically install the required packages, and start the twig server with the correct configuration options.</p><p>The <code>twig-server-packages</code> in the <code>package.json</code> <code>config</code> section can be used to specify what packages need to be installed to run the twig server. By default we already install <code>lodash</code> and <code>clsx</code>, since they are used by our included twig helpers. If you add any custom helpers that need something from a node module, you need to specify those manually.</p><div class="language-json"><pre><code><span class="token comment">// package.json</span>

<span class="token punctuation">{</span>
  ...
  <span class="token property">&quot;config&quot;</span><span class="token operator">:</span> <span class="token punctuation">{</span>
    <span class="token property">&quot;twig-support &quot;</span><span class="token operator">:</span> <span class="token boolean">false</span><span class="token punctuation">,</span>
    <span class="token property">&quot;twig-server-packages&quot;</span><span class="token operator">:</span> <span class="token punctuation">[</span><span class="token string">&quot;@mediamonks/package-name@^1.4.3&quot;</span><span class="token punctuation">]</span>
  <span class="token punctuation">}</span><span class="token punctuation">,</span>
  ...
<span class="token punctuation">}</span>
</code></pre></div>`,43),i=[s];function r(c,d,p,l,u,h){return n(),t("div",null,i)}var k=e(o,[["render",r]]);export{w as __pageData,k as default};
