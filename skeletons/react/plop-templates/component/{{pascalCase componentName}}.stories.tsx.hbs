import type { Meta } from '@storybook/react';

import {{pascalCase componentName}} from './{{pascalCase componentName}}';
import { createTemplate } from '../../../helpers/storybook/stories';
import { globalStoreDecorator } from '../../../helpers/storybook/decorators';

export default {
  title: '{{pascalCase componentName}}',
  component: {{pascalCase componentName}},
  decorators: [globalStoreDecorator],
} as Meta;

const Template = createTemplate({{pascalCase componentName}});

export const Default = Template.bind({});
Default.args = {};
