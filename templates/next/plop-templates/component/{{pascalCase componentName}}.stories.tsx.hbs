import { Meta } from '@storybook/react';

import { createTextTemplate } from '../../../storybook-helpers/stories';
import { {{pascalCase componentName}} } from './{{pascalCase componentName}}';

export default {
  title: 'Design System/{{pascalCase type}}/{{pascalCase componentName}}',
  component: {{pascalCase componentName}},
} as Meta;

const textTemplate = createTextTemplate({{pascalCase componentName}});

export const Default = textTemplate.bind({});
Default.args = {};
