import type {
  SetupTransitionSignature,
  TransitionRef,
} from '@mediamonks/muban-transition-component';

// TODO: Replace with the actual transition refs.
type TransitionRefs = Record<string, TransitionRef>;

export const setupTransitionInTimeline: SetupTransitionSignature<TransitionRefs> = () => {
  // TODO: add a timeline here
};

export const setupTransitionOutTimeline: SetupTransitionSignature<TransitionRefs> = () => {
  // TODO: add a timeline here
};
